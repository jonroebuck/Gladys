[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
domain = { path = "../domain" }  # This is how the app crate depends on the domain crate

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
